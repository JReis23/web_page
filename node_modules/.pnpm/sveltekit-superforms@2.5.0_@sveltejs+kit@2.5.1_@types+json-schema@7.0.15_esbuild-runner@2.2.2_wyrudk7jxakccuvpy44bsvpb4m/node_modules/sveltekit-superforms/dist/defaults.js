import {} from './adapters/adapters.js';
export function defaults(defaults, adapter, options) {
    if (defaults && 'superFormValidationLibrary' in defaults) {
        options = adapter;
        adapter = defaults;
        defaults = null;
    }
    const validator = adapter;
    const optionDefaults = options?.defaults ?? validator.defaults;
    return {
        id: options?.id ?? validator.id,
        valid: false,
        posted: false,
        errors: {},
        data: { ...optionDefaults, ...defaults },
        constraints: validator.constraints
    };
}
export function defaultValues(adapter) {
    return adapter.defaults;
}
